# character-sheet-maker

This repository contains a web client located in the `web/` folder.

To run the web client:

```bash
cd web
npm install
npm run dev
```

Or run from the repository root using the proxy scripts:

```bash
npm run install:web   # installs dependencies inside web/
npm run dev           # runs the web dev server (proxy to web/)
```
